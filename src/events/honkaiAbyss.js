const fs = require('fs');
const {LocalTime, ZoneOffset} = require('@js-joda/core');

/**
 * Reminder of honkai abyss time
 * */
module.exports = {
    name: 'honkaiAbyss',
    once: true,
    async execute(bot)
    {

    }
}